- name: "extract the zipped artifact"
  become: true
  unarchive:
    src: "artifact.tar.gz"
    dist: "/home/ubuntu/"
- name: "start the app"
  become: true
  shell: |
    cd /home/ubuntu/backend/dist
    npm install
    pm2 stop default
    pm2 start npm -- start

